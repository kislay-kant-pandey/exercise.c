#include<stdio.h>
int main()
{

//algebraic expressions into equivalent C statements
    // float x, y, z, a, b, c;
    // printf("enter value of x : ");
    // scanf("%f", &x);
    // printf("enter value of y : ");
    // scanf("%f", &y);

    // a=(x+3)*x*x*x;
    // b=(y-4)*(y+5);
    // z=a/b;
    // printf("%f", z);

//
// int i = 2, j = 3, k, l ;
// float a, b ;
// k = i / j * j ;
// l = j / i * i ;
// a = i / j * j ;
// b = j / i * i ;
// printf ("k :%d\nl :%d\na :%f\nb :%f\n", k, l, a, b ) ; 

//invalid variable
// int temp_in_Deg, stackqueue, $hello;
// printf("%d\t%d\t%d", &temp_in_Deg, &stackqueue, &$hello);


    return 0;
}